terraform identity

#can use profiles, terraform can link to .aws/credentials
#allows for non hard coding of credentials
#can also use other ways such as roles (#TODO look into)
provider "aws" {
  profile = "labcli"
  region  = "ca-central-1"
}

#file with .tf extension is written to provide details of resource to create
#start off with init, it will look in tf file for details of provider etc
terraform init

#to actually build the resource, needs yes confirmation
terraform apply

#show details of resource
terraform show

#for change too
#tells you what will be altered
terraform apply

#REGAN SMASH
terraform destroy
